# sample
# this content has to be added into your apps/pub/config/app.yml or equivalent

all:
  # ...
  payment:
    type: tipi
    id: 194 # numéro de client
    prod: true # false pour les tests
    salt: xxxx # très important, mettre une longue chaîne aléatoire
    url:
      payment: [https://www.jepaiemesserviceslocaux.dgfip.finances.gouv.fr/]
      uri: tpa/paiement.web
      response: cart/response
